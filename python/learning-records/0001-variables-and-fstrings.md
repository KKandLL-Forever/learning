# 掌握变量赋值与 f-string

用户在交互式解释器中独立写出了变量定义 + f-string 打印（`print(f"我是{name},今年{age}岁")`），语法完全正确。已确认掌握：变量赋值、str/int 字面量、f-string 插值。

## Evidence
第 1 课动手练习，用户主动使用了 `>>>` 交互式解释器，说明已建立 REPL 反馈环习惯。

## Implications
可以直接进入第 2 课（list 与 for 循环），无需复习变量基础。
