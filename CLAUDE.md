# Learning 工作区约定

本目录是自学课程工作区，每个子目录一门课（`claude-code-source`、`harness-engineering`、`prompt-engineering`、`python`）。

## 课程文本的写作标准

**生成或修改课程正文时，一律按 `humanizer-zh` skill 的标准来写。**

humanizer-zh quotes: 「」

skill 位置：`C:\Users\Administrator\.claude\skills\humanizer-zh\`

### 适用范围：只管文本

| 要按标准写 | 不要动 |
|---|---|
| 课程 HTML 的正文段落、box 内容、表格里的说明文字 | `<pre><code>` 里的代码 |
| 测验的题干、选项、解析、参考答案 | 代码注释（含中文注释，保持源码风格） |
| `MISSION.md`、`NOTES.md`、`RESOURCES.md`、学习记录 | 源码路径与行号引用，如 `Tool.ts:757` |
| 小标题、导航文案 | 专业术语（`tool_use`、`token`、`API`、fail-closed 等保持原样） |

### 最常踩的几条

按已经犯过的错误排序，不是按 skill 的章节顺序：

1. **长破折号 `——` 不要用。** 这是目前最重的 AI 味来源，第 0002 课初稿正文里出现了 22 次。改成逗号、句号或直接拆句。代码块内的注释除外。
2. **少用「不是……而是……」。** 需要对比时换成自然的转折或重心移动。同一段里连用两次一定要改。
3. **别用抽象抬升的小机关。** 「真正的问题是」「本质上」「归根结底」「提炼出来的原则」，这些词让句子听着有判断，落到实处往往是空的。改成具体的机制或后果。
4. **段末不要写成下集预告。** 「就是下一课」「接下来要解决的是」这类收束等于什么都没给。改成一个能立住的冷结论，或者点明还没解决什么。
5. **冒号别连着用。** 尤其避免连续几句都靠冒号展开解释。
6. **引号全篇统一用 `「」`**（嵌套 `『』`），不要和 `""` 混用。课程已有文本全部是这个样式。

### 排版细则

- 中文正文里的英文多词术语用半角空格分词，如 `AI Design Agent`
- 英文术语接中文括号时不加空格，如 `LLM（大语言模型）`
- 并列英文术语用斜杠时两侧不加空格，如 `coworkers/agents`
- 英文品牌名用官方大小写：`GitHub`、`OpenAI`、`YouTube`、`DeepSeek`
- 数字日期写作 `2026 年 8 月 9 日`

### 作者声音：默认不用

humanizer-zh 带 8 位中文作者的风格档案，可以套腔调重写。**课程正文默认走中立润色，不套任何作者的声音。** 教学材料需要跨课一致，个人腔调会让相邻两课读起来像两个人写的。

用户明确点名某位作者时才启用。

### 改写既有课程时

先备份成 `<原文件名>.orig` 再改，方便对照和回退。

改完必须验三样：

- HTML 标签配对（`<p>`/`<div>`/`<pre>`）
- 引号样式没有混用
- **测验 JS 能正常解析**，答案索引没错位。改 `explain`/`reveal` 字符串时最容易出问题

## 课程正文里要简单介绍 Node 标准库

课程代码用到 `node:fs/promises`、`node:path`、`node:crypto` 这类标准库时，**在课程正文（HTML）里用一小段说清它是干嘛的**。写在正文，不要写进代码注释 —— 代码注释留给本课的架构主题，混进 API 说明会喧宾夺主。

学习者是前端工程师，浏览器端 API 熟，Node 这边的 `stat`、`Dirent`、`resolve` 和 `join` 的区别未必张口就来。正文不提一句，读代码时就会卡在跟本课主题无关的地方。

### 位置：课程开头，正文之前

做成一个「开始之前」的预备清单，**统一列在这一课的最前面**，别拆散了埋进具体的动手环节。学习者需要的是开课前扫一眼知道待会儿会碰到什么，而不是读到某一行才被塞一段 API 说明。

放在 `lesson-sub` 和「这节课结束时你会有」之后、第一个 `<h2>` 之前。用 `box box-real` 加一张三列表（来自 / 函数 / 做什么），扫读比大段文字快。

### 写法

- **只列这一课真正用到的。** 不要把整个模块的 API 铺开，也不要提前讲下一课才出现的。
- **说用途和差别，不抄文档。** 「`join` 拼接并规范化，`resolve` 解析成绝对路径且消解 `..`」比罗列签名有用。
- **表格后面补一句它们在本课怎么配合。** 比如「先 `mkdir(dirname(full))` 再写，保证目标目录存在」，比孤立解释每个函数有用。
- 同一个 API 在后面的课里再次出现时不重复介绍。后续课程的清单开头点一句「上一课的 X、Y 继续用，不再重复」，只列**新增**的。

## 其它约定

- 语言：除专业名词外全部中文
- AI 课程的代码示例统一用 DeepSeek（OpenAI 兼容接口，`https://api.deepseek.com`，环境变量 `DEEPSEEK_API_KEY`）
- 课件是纯静态 HTML，每门课一套共享 `assets/styles.css`
